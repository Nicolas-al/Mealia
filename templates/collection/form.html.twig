{{ form_start(formCollection,  { 'attr' : { 'class': 'w-50 m-auto' }}) }}

     <div class="form-row"> 
        <div class="">    
            {{ form_row(formCollection.name, {'label': 'Collection'}) }}   
        </div>
        <div class="d-flex">
            <div class="mr-3">    
                {{ form_row(formCollection.imageFile, {'label': 'Image'}) }}   
            </div>
            <div class="col" id="zero_waste">    
                {{ form_row(formCollection.zeroWaste, {'label': 'zero-dechet'}) }}   
            </div>
        </div>
        <div>
                {{ form_row(formCollection.type, {'label': 'type'}) }}   
        </div>    
    </div>
    <button type="submit" class="btn button-form">{{ button|default('Ajouter')}}</button>

{{ form_end(formCollection) }}