{% extends 'base.html.twig' %}


{% block body %}
{% include('template.html.twig') %}
<div class="d-flex">
    {% include('admin/template.html.twig')%}
        <div class="container">
        <h3 class="text-center">Ajouter une category</h3>
                {{ include('category/form.html.twig', {form: formCategory, button : 'Ajouter'}) }}
        </div>
</div>
{{ include('footer.html.twig') }}

{% endblock %}
