{{ form_start(formType, { 'attr' : { 'id': 'form_type' }}) }}

    <div class="form-row"> 
        <div class="col-md-6">    
            {{ form_row(formType.name, {'label': 'Type'}) }}   
        </div>    
    </div>
    <button type="submit" class="btn button-form d-flex">{{ button|default('Ajouter')}}</button>


{{ form_end(formType) }}